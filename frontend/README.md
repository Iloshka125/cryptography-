# Frontend - Cryptography Platform

Frontend приложение для платформы криптографии, построенное на React + Vite.

## 📋 Требования

- **Node.js** версии 16 или выше
- **npm** или **yarn**

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd frontend
npm install
```

### 2. Настройка API

Убедитесь, что backend сервер запущен на `http://localhost:3000`. Если backend работает на другом адресе, измените `API_BASE_URL` в файлах:
- `src/api/api.js`
- `src/api/auth.js`
- `src/api/profile.js`
- `src/api/categories.js`
- `src/api/balance.js`
- `src/api/battlepass.js`

### 3. Запуск приложения

#### Режим разработки:
```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

#### Сборка для продакшена:
```bash
npm run build
```

Собранные файлы будут в папке `dist/`

#### Предпросмотр продакшен сборки:
```bash
npm run preview
```

## 📁 Структура проекта

```
frontend/
├── public/                          # Статические файлы
├── src/
│   ├── api/                         # API клиенты
│   │   ├── api.js                   # Базовый API клиент
│   │   ├── auth.js                  # Аутентификация
│   │   ├── profile.js               # Профиль пользователя
│   │   ├── categories.js            # Категории и уровни
│   │   ├── balance.js               # Баланс
│   │   ├── battlepass.js            # Battle Pass
│   │   ├── levelSettings.js         # Настройки уровней
│   │   └── levelExperienceRequirements.js  # Требования опыта
│   ├── components/                  # React компоненты
│   │   ├── enigma/                  # Компоненты для страницы Enigma
│   │   │   ├── HeaderBar.jsx
│   │   │   ├── CategoriesSection.jsx
│   │   │   ├── BattlePassSection.jsx
│   │   │   └── LeaderboardSection.jsx
│   │   ├── IconSet.jsx              # Набор иконок
│   │   └── ...
│   ├── contexts/                    # React Context
│   │   ├── AuthContext.jsx          # Контекст аутентификации
│   │   └── ToastContext.jsx         # Контекст уведомлений
│   ├── pages/                       # Страницы приложения
│   │   ├── LoginPage.jsx            # Страница входа
│   │   ├── RegisterPage.jsx         # Страница регистрации
│   │   ├── EnigmaPage.jsx            # Главная страница (игры)
│   │   ├── LevelPage.jsx              # Страница уровня
│   │   ├── ProfilePage.jsx            # Страница профиля
│   │   └── AdminPage.jsx              # Админ-панель
│   ├── styles/                      # Стили
│   │   └── global.css               # Глобальные стили
│   ├── App.jsx                      # Главный компонент
│   └── main.jsx                     # Точка входа
├── index.html                       # HTML шаблон
├── vite.config.js                   # Конфигурация Vite
└── package.json                     # Зависимости проекта
```

## 🎨 Основные функции

### Страницы

1. **LoginPage** - Вход в систему
   - Вход по email/телефону и паролю
   - Валидация полей

2. **RegisterPage** - Регистрация
   - Регистрация с никнеймом, email/телефоном и паролем
   - Проверка совпадения паролей

3. **EnigmaPage** - Главная страница игры
   - Отображение категорий и уровней
   - Battle Pass
   - Таблица лидеров
   - Навигация по уровням

4. **LevelPage** - Страница уровня
   - Отображение задания (текст или файл)
   - Ввод флага
   - Проверка ответа
   - Покупка платных уровней

5. **ProfilePage** - Профиль пользователя
   - Просмотр и редактирование профиля
   - Смена пароля
   - Выбор аватара
   - Просмотр достижений

6. **AdminPage** - Админ-панель
   - Управление категориями
   - Управление уровнями
   - Управление Battle Pass
   - Настройка требований опыта для уровней
   - Загрузка файлов заданий

### Компоненты

- **HeaderBar** - Верхняя панель навигации
- **CategoriesSection** - Секция с категориями и уровнями
- **BattlePassSection** - Секция Battle Pass
- **LeaderboardSection** - Таблица лидеров

## 🔧 Технологии

- **React 18** - UI библиотека
- **React Router** - Маршрутизация
- **Vite** - Сборщик и dev-сервер
- **React Icons** - Иконки

## 🎯 Основные возможности

### Для пользователей:
- Регистрация и вход в систему
- Прохождение уровней криптографии
- Покупка платных уровней за монеты
- Получение наград в Battle Pass
- Просмотр таблицы лидеров
- Настройка профиля и аватара
- Просмотр достижений

### Для администраторов:
- Создание и управление категориями
- Создание и управление уровнями
- Загрузка файлов заданий (.txt)
- Создание файлов заданий из текста
- Управление Battle Pass наградами
- Настройка требований опыта для уровней
- Установка платных уровней с ценой

## 📦 Зависимости

### Основные:
- **react** - React библиотека
- **react-dom** - React DOM рендерер
- **react-router-dom** - Маршрутизация

### Разработка:
- **vite** - Сборщик
- **@vitejs/plugin-react** - Плагин React для Vite

## 🎨 Стилизация

Проект использует:
- **Tailwind CSS** (через CDN) - Utility-first CSS фреймворк
- Кастомные стили в `src/styles/global.css`
- Кастомные скроллбары и чекбоксы

## 🔌 API Интеграция

Все API запросы находятся в папке `src/api/`. Каждый модуль содержит функции для работы с соответствующими эндпоинтами backend.

### Пример использования:

```javascript
import { getCategories } from './api/categories';

const categories = await getCategories(userId);
```

## 🛠️ Разработка

### Добавление новой страницы:

1. Создайте компонент в `src/pages/`
2. Добавьте маршрут в `src/App.jsx`
3. Добавьте навигацию при необходимости

### Добавление нового API метода:

1. Откройте соответствующий файл в `src/api/`
2. Добавьте новую функцию экспорта
3. Используйте базовый `apiRequest` или создайте кастомный запрос

## ⚠️ Важные замечания

1. Убедитесь, что backend сервер запущен перед запуском frontend
2. Проверьте правильность `API_BASE_URL` во всех API файлах
3. Для продакшена измените `API_BASE_URL` на адрес продакшен сервера
4. При изменении порта Vite (по умолчанию 5173) обновите CORS настройки в backend

## 🐛 Отладка

- Откройте DevTools браузера (F12)
- Проверьте вкладку Console на наличие ошибок
- Проверьте вкладку Network для просмотра API запросов
- Убедитесь, что backend сервер отвечает на запросы

## 📱 Адаптивность

Приложение адаптировано для работы на:
- Десктопах (1920px+)
- Планшетах (768px+)
- Мобильных устройствах (320px+)

## 🚀 Деплой

### Сборка для продакшена:

```bash
npm run build
```

### Предпросмотр сборки:

```bash
npm run preview
```

Собранные файлы в `dist/` можно развернуть на любом статическом хостинге:
- Vercel
- Netlify
- GitHub Pages
- AWS S3 + CloudFront
- И другие

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что backend сервер запущен
3. Проверьте правильность API URL
4. Проверьте CORS настройки в backend
