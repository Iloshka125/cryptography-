# Backend - Cryptography Platform API

Backend сервер для платформы криптографии с системой уровней, достижений, Battle Pass и админ-панелью.

## 📋 Требования

- **Node.js** версии 16 или выше
- **PostgreSQL** версии 12 или выше
- **npm** или **yarn**

## 🚀 Быстрый старт

### Вариант 1: Запуск с Docker (Рекомендуется) 🐳

Самый простой способ - использовать Docker Compose из корня проекта:

```bash
# Из корня проекта
docker-compose up -d

# Или только backend + PostgreSQL для разработки
docker-compose -f docker-compose.dev.yml up -d
```

Backend будет доступен на `http://localhost:3000`

### Вариант 2: Локальный запуск

#### 1. Установка зависимостей

```bash
cd backend
npm install
```

#### 2. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE cryptography_db;
```

#### 3. Настройка переменных окружения

Создайте файл `.env` в папке `backend` со следующим содержимым:

```env
# База данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=cryptography_db

# Порт сервера
PORT=3000
```

**Важно:** Замените `your_username` и `your_password` на ваши реальные учетные данные PostgreSQL.

#### 4. Запуск сервера

##### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

##### Режим продакшена:
```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## 📁 Структура проекта

```
backend/
├── app.js                          # Главный файл приложения
├── config/
│   ├── database.js                 # Конфигурация подключения к БД
│   └── upload.js                   # Конфигурация multer для загрузки файлов
├── middleware/
│   └── auth.js                     # Middleware для аутентификации
├── migrations/                     # Миграции базы данных
│   ├── createUsersTable.js
│   ├── createCategoriesAndLevelsTables.js
│   ├── addTaskFileFieldToLevels.js
│   └── ...
├── models/                         # Модели данных
│   ├── User.js
│   ├── Level.js
│   ├── Category.js
│   ├── Balance.js
│   ├── BattlePass.js
│   └── ...
├── routes/                         # API маршруты
│   ├── auth.js                     # Аутентификация (регистрация, вход)
│   ├── profile.js                  # Профиль пользователя
│   ├── categories.js               # Категории и уровни
│   ├── balance.js                  # Баланс (монеты, подсказки)
│   ├── battlepass.js               # Battle Pass
│   ├── leaderboard.js              # Таблица лидеров
│   ├── levelSettings.js            # Настройки уровней
│   └── levelExperienceRequirements.js  # Требования опыта для уровней
└── uploads/                        # Загруженные файлы заданий
    └── tasks/
```

## 🔌 API Эндпоинты

### Аутентификация (`/auth`)

- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему

### Профиль (`/profile`)

- `POST /profile/get` - Получить профиль пользователя
- `POST /profile/update` - Обновить профиль
- `POST /profile/change-password` - Изменить пароль
- `POST /profile/unlock-achievement` - Разблокировать достижение

### Категории и уровни (`/categories`)

- `GET /categories` - Получить все категории с уровнями
- `GET /categories/:id` - Получить категорию по ID
- `POST /categories` - Создать категорию (только админ)
- `PUT /categories/:id` - Обновить категорию (только админ)
- `DELETE /categories/:id` - Удалить категорию (только админ)
- `GET /categories/levels/:id` - Получить уровень по ID
- `POST /categories/:categoryId/levels` - Создать уровень (только админ)
- `PUT /categories/levels/:id` - Обновить уровень (только админ)
- `DELETE /categories/levels/:id` - Удалить уровень (только админ)
- `POST /categories/levels/:id/check` - Проверить флаг уровня
- `POST /categories/levels/:id/purchase` - Купить платный уровень
- `GET /categories/levels/:id/task-file` - Скачать файл задания

### Баланс (`/balance`)

- `POST /balance/get` - Получить баланс пользователя
- `POST /balance/update-coins` - Обновить количество монет
- `POST /balance/update-hints` - Обновить количество подсказок
- `POST /balance/add-coins` - Добавить монеты
- `POST /balance/subtract-coins` - Вычесть монеты
- `POST /balance/add-hints` - Добавить подсказки
- `POST /balance/subtract-hints` - Вычесть подсказки

### Battle Pass (`/battlepass`)

- `GET /battlepass` - Получить все награды Battle Pass
- `POST /battlepass/:id/claim` - Получить награду
- `GET /battlepass/:id` - Получить награду по ID
- `POST /battlepass` - Создать награду (только админ)
- `PUT /battlepass/:id` - Обновить награду (только админ)
- `DELETE /battlepass/:id` - Удалить награду (только админ)

### Лидерборд (`/leaderboard`)

- `GET /leaderboard` - Получить таблицу лидеров

### Настройки уровней (`/level-settings`)

- `GET /level-settings` - Получить настройки уровней
- `POST /level-settings/update` - Обновить настройки уровней (только админ)

### Требования опыта (`/level-experience-requirements`)

- `GET /level-experience-requirements` - Получить все требования опыта
- `GET /level-experience-requirements/:levelNumber` - Получить требование для уровня
- `POST /level-experience-requirements/set` - Установить требование опыта (только админ)
- `DELETE /level-experience-requirements/:levelNumber` - Удалить требование (только админ)

## 🗄️ База данных

Проект использует PostgreSQL. Все таблицы создаются автоматически при первом запуске через миграции.

### Основные таблицы:

- `users` - Пользователи
- `categories` - Категории заданий
- `levels` - Уровни заданий
- `user_level_progress` - Прогресс пользователей по уровням
- `user_purchased_levels` - Купленные платные уровни
- `user_balance` - Баланс пользователей (монеты, подсказки)
- `battle_pass` - Награды Battle Pass
- `user_battle_pass` - Полученные награды пользователями
- `user_achievements` - Достижения пользователей
- `level_experience_requirements` - Требования опыта для уровней

## 📦 Зависимости

- **express** - Веб-фреймворк
- **pg** - PostgreSQL клиент
- **bcrypt** - Хеширование паролей
- **multer** - Загрузка файлов
- **dotenv** - Переменные окружения

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- Пустые строки для email/phone конвертируются в NULL для избежания конфликтов уникальности
- Файлы заданий хранятся в защищенной директории
- CORS настроен для работы только с фронтендом

## 📝 Загрузка файлов

Система поддерживает загрузку `.txt` файлов для заданий уровней:
- Максимальный размер файла: 10MB
- Разрешенные форматы: `.txt`
- Файлы сохраняются в `uploads/tasks/`

## 🛠️ Разработка

### Добавление новой миграции

1. Создайте файл в папке `migrations/`
2. Экспортируйте async функцию
3. Добавьте вызов миграции в `app.js`

Пример:
```javascript
const pool = require('../config/database');

async function myMigration() {
  try {
    await pool.query('ALTER TABLE users ADD COLUMN new_field VARCHAR(255)');
    console.log('✅ Миграция выполнена');
  } catch (err) {
    console.error('❌ Ошибка миграции:', err.stack);
    process.exit(1);
  }
}

module.exports = myMigration;
```

## ⚠️ Важные замечания

1. Убедитесь, что PostgreSQL запущен перед запуском сервера
2. Проверьте правильность настроек в `.env` файле
3. При первом запуске все миграции выполнятся автоматически
4. Для продакшена используйте переменные окружения вместо хардкода

## 📞 Поддержка

При возникновении проблем проверьте:
- Логи сервера в консоли
- Подключение к базе данных
- Правильность переменных окружения
- Наличие всех зависимостей

