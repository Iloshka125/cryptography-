# Cryptography Platform

Платформа для обучения криптографии с системой уровней, достижений, Battle Pass и админ-панелью.

## 📖 Описание

Это полнофункциональная платформа для изучения криптографии, где пользователи могут:
- Проходить уровни с заданиями по криптографии
- Покупать платные уровни за монеты
- Получать награды в Battle Pass
- Соревноваться в таблице лидеров
- Настраивать свой профиль и получать достижения

Администраторы могут:
- Создавать и управлять категориями и уровнями
- Загружать файлы заданий (.txt)
- Управлять Battle Pass наградами
- Настраивать требования опыта для уровней

## 🏗️ Архитектура

Проект состоит из двух частей:

- **Backend** - Node.js + Express + PostgreSQL API сервер
- **Frontend** - React + Vite веб-приложение

## 📋 Требования

### Общие:
- **Node.js** версии 16 или выше
- **npm** или **yarn**

### Для Backend:
- **PostgreSQL** версии 12 или выше

## 🚀 Быстрый старт

### Вариант 1: Запуск с Docker (Рекомендуется) 🐳

Самый простой способ запустить весь проект одной командой:

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd cryptography-

# Запустите все сервисы (PostgreSQL, Backend, Frontend)
docker-compose up -d

# Проверьте статус контейнеров
docker-compose ps

# Просмотрите логи
docker-compose logs -f
```

После запуска:
- **Frontend**: http://localhost
- **Backend API**: http://localhost:3000
- **PostgreSQL**: localhost:5432

Остановка всех сервисов:
```bash
docker-compose down
```

Остановка с удалением данных:
```bash
docker-compose down -v
```

#### Режим разработки с Docker:

Для разработки с hot-reload используйте:

```bash
# Запустите только PostgreSQL и Backend
docker-compose -f docker-compose.dev.yml up -d

# Frontend запускайте локально для лучшего опыта разработки
cd frontend
npm install
npm run dev
```

### Вариант 2: Локальный запуск (без Docker)

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd cryptography-
```

#### 2. Настройка Backend

```bash
# Перейдите в папку backend
cd backend

# Установите зависимости
npm install

# Создайте базу данных PostgreSQL
# В psql или другом клиенте:
CREATE DATABASE cryptography_db;

# Создайте файл .env
cp .env.example .env  # Если есть пример
# Или создайте .env вручную со следующим содержимым:
```

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=cryptography_db
PORT=3000
```

```bash
# Запустите backend сервер
npm run dev
```

Backend будет доступен на `http://localhost:3000`

#### 3. Настройка Frontend

```bash
# Откройте новый терминал и перейдите в папку frontend
cd frontend

# Установите зависимости
npm install

# Запустите frontend приложение
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## 📁 Структура проекта

```
cryptography-/
├── backend/                    # Backend сервер
│   ├── app.js                  # Главный файл приложения
│   ├── config/                 # Конфигурация
│   ├── migrations/              # Миграции БД
│   ├── models/                  # Модели данных
│   ├── routes/                  # API маршруты
│   ├── middleware/              # Middleware
│   └── uploads/                 # Загруженные файлы
│
├── frontend/                    # Frontend приложение
│   ├── src/
│   │   ├── api/                 # API клиенты
│   │   ├── components/          # React компоненты
│   │   ├── contexts/            # React Context
│   │   ├── pages/               # Страницы
│   │   └── styles/              # Стили
│   └── public/                  # Статические файлы
│
└── README.md                    # Этот файл
```

## 🔧 Основные команды

### Docker команды:

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр логов
docker-compose logs -f [service_name]  # service_name: postgres, backend, frontend

# Пересборка контейнеров после изменений
docker-compose build
docker-compose up -d

# Остановка и удаление всех данных (включая БД)
docker-compose down -v

# Запуск в режиме разработки (только PostgreSQL + Backend)
docker-compose -f docker-compose.dev.yml up -d
```

### Backend (локально):

```bash
cd backend

# Установка зависимостей
npm install

# Запуск в режиме разработки (с автоперезагрузкой)
npm run dev

# Запуск в режиме продакшена
npm start
```

### Frontend (локально):

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

## 📚 Документация

Подробная документация доступна в соответствующих README файлах:

- [Backend README](./backend/README.md) - Документация по backend API
- [Frontend README](./frontend/README.md) - Документация по frontend приложению
- [Docker Guide](./DOCKER.md) - Подробное руководство по Docker

## 🔌 API

Backend предоставляет RESTful API на `http://localhost:3000`

### Основные эндпоинты:

- `/auth` - Аутентификация (регистрация, вход)
- `/profile` - Профиль пользователя
- `/categories` - Категории и уровни
- `/balance` - Баланс (монеты, подсказки)
- `/battlepass` - Battle Pass
- `/leaderboard` - Таблица лидеров
- `/level-settings` - Настройки уровней
- `/level-experience-requirements` - Требования опыта

Подробная документация API доступна в [Backend README](./backend/README.md).

## 🗄️ База данных

Проект использует PostgreSQL. Все таблицы создаются автоматически при первом запуске backend через миграции.

### Основные таблицы:

- `users` - Пользователи
- `categories` - Категории заданий
- `levels` - Уровни заданий
- `user_level_progress` - Прогресс пользователей
- `user_purchased_levels` - Купленные уровни
- `user_balance` - Баланс пользователей
- `battle_pass` - Награды Battle Pass
- `user_battle_pass` - Полученные награды
- `user_achievements` - Достижения
- `level_experience_requirements` - Требования опыта

## 🎯 Основные функции

### Для пользователей:
- ✅ Регистрация и вход в систему
- ✅ Прохождение уровней криптографии
- ✅ Покупка платных уровней
- ✅ Получение наград в Battle Pass
- ✅ Просмотр таблицы лидеров
- ✅ Настройка профиля и аватара
- ✅ Просмотр достижений
- ✅ Скачивание файлов заданий

### Для администраторов:
- ✅ Создание и управление категориями
- ✅ Создание и управление уровнями
- ✅ Загрузка файлов заданий (.txt)
- ✅ Создание файлов заданий из текста
- ✅ Управление Battle Pass наградами
- ✅ Настройка требований опыта для уровней
- ✅ Установка платных уровней с ценой

## 🛠️ Технологии

### Backend:
- **Node.js** - JavaScript runtime
- **Express** - Веб-фреймворк
- **PostgreSQL** - База данных
- **bcrypt** - Хеширование паролей
- **multer** - Загрузка файлов

### Frontend:
- **React** - UI библиотека
- **Vite** - Сборщик
- **React Router** - Маршрутизация
- **Tailwind CSS** - Стилизация

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- CORS настроен для работы только с frontend
- Валидация данных на backend
- Защита от SQL инъекций через параметризованные запросы

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад в проект

При внесении изменений:
1. Создайте новую ветку
2. Внесите изменения
3. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем:

1. Проверьте, что все зависимости установлены
2. Убедитесь, что PostgreSQL запущен
3. Проверьте правильность настроек в `.env`
4. Проверьте логи в консоли backend и frontend
5. Убедитесь, что порты 3000 и 5173 свободны

## 🚀 Деплой

### С Docker:

```bash
# На продакшен сервере
git clone <repository-url>
cd cryptography-

# Настройте переменные окружения в docker-compose.yml или через .env файлы
# Запустите
docker-compose up -d

# Для обновления
git pull
docker-compose build
docker-compose up -d
```

### Без Docker:

#### Backend:
- Настройте переменные окружения на сервере
- Убедитесь, что PostgreSQL доступен
- Запустите `npm start`

#### Frontend:
- Выполните `npm run build`
- Разверните папку `dist/` на статическом хостинге
- Обновите `API_BASE_URL` на адрес продакшен backend

## 📄 Changelog

### Версия 1.0.0
- Базовая функциональность
- Система уровней и категорий
- Battle Pass
- Таблица лидеров
- Админ-панель
- Загрузка файлов заданий

---

**Приятного использования! 🎉**

